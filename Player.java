// Player class

public class Player {

    // points field
    private int points = 0;

    // no constructor necessary because points always start at 0

    // method to increment points
    public void incrementPoints() {
        points++;
    }

    // method to getPoints
    public int getPoints() {
        return points;
    }
}
